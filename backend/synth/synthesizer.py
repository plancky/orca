from backend.orchestration.models.results import TaskResult


async def synthesize(
    intent, node_outputs: dict, pending_actions=None, llm_client=None
) -> TaskResult:
    """Stub — Wave C4 fills this."""
    raise NotImplementedError("Wave C4 fills this")
